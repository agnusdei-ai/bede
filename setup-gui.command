#!/usr/bin/env bash
# Double-click this in Finder to set up Bede with no typing required — it
# opens a form in your browser instead of a terminal. See
# docs/PRODUCTION_SETUP.md if you'd rather use the terminal-based setup.sh.
cd "$(dirname "$0")"
bash scripts/setup-gui-common.sh
