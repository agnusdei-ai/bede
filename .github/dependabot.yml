version: 2

# Dependabot version updates are DISABLED (empty `updates:` list) — two
# separate incomplete bumps landed on main the same day (PR #267 moved
# react to 19.x without react-dom, PR #250 did the mirror-image in /demo;
# PR #254/#266 auto-merged a breaking tailwindcss 3->4 major with no
# migration), each individually plausible-looking but breaking `npm ci`/
# the production build once merged. See PR #274 for the fix and the fuller
# incident writeup.
#
# This trades away the automated CVE-watch PR #274's dependabot.yml
# grouping/major-version-guard follow-up was trying to preserve (see
# docs/SECURITY.md's "Known open gaps" — that gap is now open again).
# Dependabot's *security alerts* (the Security tab / vulnerability
# banner) are a separate GitHub feature from version-update PRs and are
# unaffected by this file either way — only the automated "bump this
# dependency" PRs are turned off here. Re-enabling later just means
# restoring a non-empty `updates:` list (see PR #274's diff for the
# grouped/guarded version that was in place before this).
updates: []
