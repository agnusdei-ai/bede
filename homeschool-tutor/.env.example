# No secrets here — all API calls go through the backend proxy
# Vite env vars (must start with VITE_) for optional feature flags
VITE_SESSION_DURATION_MINUTES=120

# Optional: UI language, baked in at build time — must match the backend's
# LOCALE env var. See docs/LOCALIZATION.md.
# VITE_LOCALE=es
