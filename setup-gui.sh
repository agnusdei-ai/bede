#!/usr/bin/env bash
# Run this to set up Bede with no typing required beyond this one command —
# it opens a form in your browser instead of a terminal. See
# docs/PRODUCTION_SETUP.md if you'd rather use the terminal-based setup.sh.
cd "$(dirname "$0")"
bash scripts/setup-gui-common.sh
